"""Unit tests for individual modules.

Separate from `evals/`, which holds the three assignment deliverables and
asserts behaviour of the assembled system. Nothing here touches the network:
the live OpenRouter smoke test is marked `live` and deselected by default.
"""
